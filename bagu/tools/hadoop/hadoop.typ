- hadoop是什么？
  - 如果用mysql处理海量数据，mysql扛不住TB以上的数据量，hadoop可以处理PB级别的数据量。
- HDFS
  - 大文件无法在一个硬盘上存储
  - 于是我们将大文件切分成一个个128MB的小块，分布式存储在集群的各个节点上
    - 128MB可以提升磁盘利用率
  - 并且为了保证数据不丢失，每个数据块会有多个副本，默认是3个，存放在不同的节点上
  - namenode：负责管理文件系统的元数据，记录每个文件的数据块位置
    - namespcae：文件和目录的层次结构，存储文件的读写权限，文件名，大小，地址
    - blockmanager：管理数据块存储的位置
    - 备用namenode：定期从namenode获取元数据的镜像，防止namenode故障时数据丢失
  - datanode：负责存储实际的数据块，处理读写请求
  - 高性能：内存读写
  - 持久化：
    - fsimage：定期将内存中的元数据写入磁盘，防止数据丢失
    - editlog：记录每次对文件系统的修改操作，保证数据一致性
  - 可扩展：
    - 根据业务属性拆分namespace里的目录树
    - 不同目录树可以分配给不同的namenode管理

- MapReduce
  - 如果计算一个TB的数据，单机处理需要很长时间
  - hadoop将数据分布在集群的各个节点上，每个节点处理自己本地的数据块
  - Map阶段：将输入数据切分成小块，分发给各个节点进行处理，生成中间结果
  - Reduce阶段：将中间结果进行汇总，得到最终结果
  - 用户只需要编写Map和Reduce函数，hadoop负责调度和执行

- YARN
  - Map任务和Reduce任务需要资源来运行
  - 这么多任务如何协调运行？就需要YARN来统一进行cpu和内存的分配
  - 资源管理器，负责集群资源的分配和调度
  - 监控各个节点的资源使用情况，确保任务能够高效运行
- Hadoop生态系统
  - Hive：类SQL查询引擎，方便数据分析
    - 支持SQL语法，适合数据分析人员使用
    - 将SQL查询转换为MapReduce任务执行
  - Spark：内存计算框架，适合实时处理
    - 每次数据分析都需要Hive解析SQL，生成MapReduce任务，启动JVM，加载数据，执行计算，写回结果，这个过程开销很大
    - Spark将数据加载到内存中，进行多次计算，避免频繁的磁盘I/O，提高计算速度
    - 支持批处理和流处理
    - 提供丰富的API，支持多种编程语言
  - Flink：流处理框架，适合实时数据处理
    - 因为spark更适合批处理，实时处理方面不如Flink
    - Flink从设计之初就支持流处理，能够低延迟、高吞吐地处理实时数据
  - HBase：分布式NoSQL数据库，适合随机读写
    - 在海量数据中进行随机读写操作
    - 支持高并发读写，适合在线应用

- 写入流程
  - 客户端将数据写入HDFS时，首先将数据切分成多个块
  - 每个块会被分配到多个数据节点上进行存储
  - 数据节点会将数据块写入本地磁盘，并将写入状态反馈给客户端
  - 客户端在收到所有数据块的写入确认后，才会认为写入操作成功完成

- 读取流程
  - 写sql,输入到hive
  - hive将sql解析成多个MapReduce任务
  - YARN将任务分发到各个节点上运行
  - 每个节点读取本地数据块，进行计算，生成中间结果
  - Reduce节点汇总中间结果，得到最终结果
  - 如果追求性能，可以将hive换成sparksql,将mapreduce换成spark任务，通过内存计算提高性能
  - 对于实时性要求更高的场景，可以使用Flink进行流式处理
  - 如果是在线实时查询，可以将数据存储在HBase中，进行快速随机读写操作

- Hbase为什么支持大数据的快速查询
  - 基于 HDFS 的分布式存储，天然可扩展
  - 按 RowKey 排序的 LSM-Tree（日志结构合并树）结构
    - 写入只需要 顺序写磁盘（append），不需要随机 IO → 写非常快
    - 查询时通过 MemStore + HFile 索引 + BloomFilter 快速定位数据
  - 按 RowKey 范围划分 Region → 可并行查询
    - 也就是说，一个表数据会被划分为多个region, 每个region存储在不同的RegionServer上
    - 查询时可以并行访问多个RegionServer，提高查询速度
  - 内存（MemStore）+ BlockCache 提升读写性能
  - 强大的随机读写能力