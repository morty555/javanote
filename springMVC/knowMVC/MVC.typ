1.三层模型和MVC

#image("threemodel.png")

三层模型为：持久层，业务层，表现层

#image("MVC.png")

MVC为 model vision controller

共同点：分层结构降低耦合度，提高扩展力。

区别：三层模型关注业务逻辑组件的划分。MVC关注的是整个应用程序的层次关系和分离思想，现在都是结合的

#image("MVCDO.png")

#image("MVCadvantages.png")

在springMVC框架中，脱离了WEB容器和TOMCAT也可以做单元测试
